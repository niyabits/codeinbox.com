---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...props } = Astro.props;
---

<div class:list={["text-sm text-gray-500", className]} {...props}>
  <slot />
</div>
