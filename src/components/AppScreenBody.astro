---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...props } = Astro.props;
---

<div class:list={["mt-6 flex-auto rounded-t-2xl bg-white", className]} {...props}>
  <slot />
</div>
